# Quick Cards: built-in notes for interview support

## Initial setup

Create or activate virtual environment (Python):
```bash
virtualenv venv # create
source venv/bin/activate # active
pip install -r requirements.txt # install requirements
```